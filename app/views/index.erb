<ul>
  <% @recipes.each do |recipe| %>
    <li>
      <a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a>
      <%= recipe.ingredients %>
      <%= recipe.cook_time %> 
      <a href="<%="/recipes/#{recipe.id}/edit" %>"/edit">Edit</a>
    </li>
  <% end %>
</ul>

<a href ="/recipe/new">Add a Recipie</a>
